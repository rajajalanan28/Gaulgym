import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Tentang | Gaul Gym', description: 'Pelajari visi, misi, dan cerita di balik Gaul Gym — pusat kebugaran premium no.1 di Jakarta.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
