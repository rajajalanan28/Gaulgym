import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Daftar | Gaul Gym', description: 'Daftar menjadi member Gaul Gym dan mulai transformasi kebugaran Anda hari ini.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
