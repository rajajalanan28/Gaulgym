import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Fasilitas | Gaul Gym', description: 'Jelajahi fasilitas premium Gaul Gym: area free weights, cardio, sauna, studio kelas, dan lounge eksekutif.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
